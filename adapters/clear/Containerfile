FROM registry.access.redhat.com/ubi9/python-311:latest

WORKDIR /app

USER 0
# clear-eval[tool-calls] from PyPI may pull compiled dependencies that need build tools.
# (ubi9/python-311 provides dnf; microdnf is not guaranteed.)
RUN dnf install -y git && dnf clean all

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY main.py .
# Imported by main (RED_HAT_CLEAR_DASHBOARD_CSS, RED_HAT_DASHBOARD_JS_PATCHES); not fetched remotely.
COPY themes ./themes

ENV PYTHONUNBUFFERED=1 EVALHUB_MODE=k8s

USER 1001

ENTRYPOINT ["python", "main.py"]
